
#Define variables
filename = "./ressources/ref-sentences.txt"


#Reading lines and storing them in a list
def read_reference_text(filename):
    f = open(filename)
    for line in f:
        print(line)
    f.close()
    #sentence_list = []
    #return

#
def make_word_vector(w, list):
    word_dictionary = ""
    return word_dictionary

#We execute the file
read_reference_text(filename)